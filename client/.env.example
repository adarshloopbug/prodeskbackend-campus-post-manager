# ==============================================================================
# FRONTEND CLIENT ENVIRONMENT CONFIGURATION
# ==============================================================================
# The base REST API URL pointing to the Express backend
# In Vite, environment variables exposed to client code MUST start with VITE_
VITE_API_URL=http://localhost:5000/api/posts
