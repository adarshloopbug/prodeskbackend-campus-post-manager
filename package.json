{
  "name": "campus-posts-manager",
  "version": "1.0.0",
  "description": "Fullstack MERN Campus Posts Manager",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1",
    "multer": "^1.4.5-lts.1"
  }
}
